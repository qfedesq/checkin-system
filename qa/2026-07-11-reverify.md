# QA re-verificación — 2026-07-11 (post v0.33)

Orquestado con Sonnet 5: 15 bugs del cliente re-verificados + 7 dimensiones + verificación adversarial.

## Bugs del cliente: 15/15 RESUELTOS
copy corrido/franco mensual · mis-solicitudes completa · rango lunes-domingo · subida no colgada · otros-documentos recuadro/solapamiento · cerrar sesión · select dark · cambiar rol · copy francos admin · entregas paso Enviar · mini-calendario meses lejanos · fecha ingreso local · alertas orden proximidad · calendario español · notificaciones 30d/diarias/auto-bloqueo.
Nota: el ImageSlot del admin (EmployeeDetailClient) NO tiene el try/catch/finally que sí tienen los del empleado — completar por consistencia (no es el caso reportado por el cliente).

## Hallazgos NUEVOS (confirmados)

### Seguridad / correctitud — seguros de arreglar
1. (CRIT) LoginForm: signOut tras fallo de WebAuthn no cierra sesión (falta CSRF) → queda logueado. Usar signOutAction. [src/app/login/LoginForm.tsx]
2. (CRIT) Rutas de empleado que usan auth() crudo no revalidan estado/rol contra DB (p.ej. /api/leaves). Usar requireActiveUser. [src/app/api/leaves/route.ts]
3. (CRIT) deliveries/[id]/open confía en el rol stale del JWT para acceso cross-user. [src/app/api/deliveries/[id]/open/route.ts]
4. (MAJOR) webauthn/register/verify auto-aprueba dispositivos según rol stale del JWT. [src/app/api/webauthn/register/verify/route.ts]
5. (MAJOR) leaves.ts no valida server-side que la fecha de inicio sea futura (se puede pedir en el pasado). [src/lib/leaves.ts]
6. (MAJOR) /api/uploads/signature: dead code sin auth que regresa el hardening QA-002/034 → eliminar si está huérfano. [src/app/api/uploads/signature/route.ts]
7. (MAJOR) admin/attendance (pantalla) parsea fechas UTC-naive mientras el export usa ART → pantalla ≠ export. [src/app/admin/attendance/page.tsx]
8. (MAJOR) Colores de vacaciones/francos del calendario casi ilegibles en modo claro (calibrados para dark). [CalendarClient.tsx, AdminMiniCalendar.tsx]
9. (MAJOR) Errores de geolocalización/WebAuthn en inglés en el check-in diario. [src/app/(app)/checkin/CheckinClient.tsx]
10. (MAJOR) Edición admin permite category=DRIVER sin professionalLicenseExpiry → ese chofer escapa al monitoreo del cron. [src/app/api/admin/employees/[id]/route.ts]
11. (MAJOR) TOCTOU en "último admin activo" (role change) puede dejar 0 admins. [src/app/api/admin/users/[id]/role/route.ts]
12. (MINOR) approve (usuarios) sin catch P2002 → 500 en colisión de legajo. checkout sin guard idempotencia. checkin sin manejo P2002. expiry: DocumentUpload sin push y sin filtrar ACTIVE. franco no valida solape con vacaciones propias.
13. (MINOR) AuditLog sin visor en la app. JWT sin maxAge (default 30d; mitigado por requireActiveUser). middleware redirige /api de PENDING a /pending (rompe JSON).

### Requieren decisión (cambian comportamiento / arquitectura)
A. (CRIT) Check-in NO verifica WebAuthn del lado del servidor: sólo exige sesión + dispositivo aprobado. Enforcement real = challenge/response por fichada (rediseño). La app viene funcionando así.
B. (CRIT) Límite ~4.5 MB de Vercel hace inalcanzables los límites 8/10/15 MB declarados → subidas de fotos grandes de iPhone fallan (explica los "Subiendo…" y fallas de carga). Fix: compresión/redimensión client-side antes de subir, o subida directa a Vercel Blob.
